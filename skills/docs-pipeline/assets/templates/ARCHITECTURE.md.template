# ARCHITECTURE.md

> 本文件由 docs-pipeline skill 在初始化时通过探索目标项目自动生成。
> 探索失败时落地为本骨架，由用户手动填充。

## 项目概述

<一句话项目定位>。

- **后端**: <技术栈，如 FastAPI + SQLModel + SQLite，无则填"无">
- **前端**: <技术栈，如 Vue 3 + TypeScript + Vite，无则填"无">
- **数据库**: <数据库类型与位置，无则填"无">

## 常用命令

### 后端

```bash
<安装依赖命令>
<启动命令>
<测试命令>
```

### 前端

```bash
<安装依赖命令>
<开发服务器命令>
<构建命令>
```

### 完整启动

<开发模式说明，如代理配置、端口分配>。

## 架构

```
<目录树，列出 src/、app/、apps/ 等核心源码目录的二级结构与每个目录的职责注释>
```

## 数据模型

<核心数据模型清单，按模块分组。纯前端/CLI 项目填"无（不涉及持久化数据模型）">

- `<模型名>` - <一句话职责>
- ...

## 开发注意事项

- <数据库迁移策略，如有>
- <开发代理配置，如有>
- <路由/入口配置位置>
- <其他需要新人知道的关键约定>
